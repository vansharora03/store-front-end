import NavBar from './NavBar';

export default function Shop() {
    return (
        <div className="Shop">
            <NavBar/>
            <h1 className="page-title">Shop</h1>
            
        </div>
    );
}